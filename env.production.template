# Elite Tutors Hub - Production Environment Template
# Copy this file to .env on your VPS and update the values

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=4000
BASE_URL=https://api.yourdomain.com
FRONTEND_URL=https://yourdomain.com
TIMEZONE=Asia/Beirut

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=tutoring
DB_USER=postgres
DB_PASS=CHANGE_THIS_TO_A_SECURE_PASSWORD

# ===========================================
# JWT CONFIGURATION
# ===========================================
# Generate strong secrets (minimum 32 characters each)
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_JWT_SECRET_MINIMUM_32_CHARACTERS
JWT_EXPIRES_IN=1d
REFRESH_SECRET=CHANGE_THIS_TO_A_STRONG_REFRESH_SECRET_MINIMUM_32_CHARACTERS
REFRESH_EXPIRES_IN=7d

# ===========================================
# SMTP CONFIGURATION (HOSTINGER)
# ===========================================
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true

# Email Accounts - Update passwords if needed
SMTP_ADMIN_EMAIL=admin@elitetutorshub.net
SMTP_ADMIN_PASSWORD=n*1YqDNJ

SMTP_SCHEDULE_EMAIL=schedule@elitetutorshub.net
SMTP_SCHEDULE_PASSWORD=hk?Wa1X$

SMTP_SUPPORT_EMAIL=support@elitetutorshub.net
SMTP_SUPPORT_PASSWORD=7t^MwabXJ;r&

SMTP_FINANCE_EMAIL=finance@elitetutorshub.net
SMTP_FINANCE_PASSWORD=6I&2xXLuy&5O

SMTP_FEEDBACK_EMAIL=feedback@elitetutorshub.net
SMTP_FEEDBACK_PASSWORD=C11U>&2On!

NO_REPLY_EMAIL=no-reply@elitetutorshub.net

# ===========================================
# STRIPE CONFIGURATION (OPTIONAL)
# ===========================================
# Only add these if you're using Stripe payments
STRIPE_SECRET=sk_live_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# ===========================================
# SECURITY NOTES
# ===========================================
# 1. CHANGE ALL PASSWORDS to strong, unique values
# 2. Generate new JWT secrets (minimum 32 characters each)
# 3. Update domain names to your actual domains
# 4. Keep this file secure and never commit it to version control
# 5. Use environment-specific values for production
# 6. Consider using a secrets management service for production

# ===========================================
# DEPLOYMENT CHECKLIST
# ===========================================
# [ ] Database password changed
# [ ] JWT secrets generated and updated
# [ ] Domain names updated
# [ ] Stripe keys configured (if using payments)
# [ ] Email passwords verified
# [ ] SSL certificates configured
# [ ] Firewall rules configured
# [ ] Backup strategy implemented
